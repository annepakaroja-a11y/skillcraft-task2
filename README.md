# skillcraft-task2
This task involves creating a number guessing game in Python. The program randomly generates a number, asks the user to guess it, compares the guess with the generated number, and provides hints such as “Too high” or “Too low” until the correct number is guessed.
